# Verified with Python 3.12.2 on macOS (arm64).
# pyreadr is needed only for the ANES application (reads the .rds file).
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
matplotlib==3.9.2
pyreadr==0.5.6

# For experiments.ipynb. reproduce.sh does not need these.
# jinja2 is what pandas uses to render the styled summary tables.
notebook==7.2.2
nbconvert==7.16.4
ipykernel==6.28.0
jinja2==3.1.4
